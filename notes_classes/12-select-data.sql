-- SELECT * FROM users;
SELECT AVG(yearly_salary) FROM users;
-- AVG() is a function that exists both in MySQL and PostgreSQL which calculates the average sum of numbers. In this case, the average yearly salary from the users; 